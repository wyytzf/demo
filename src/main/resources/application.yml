spring:
  datasource:
    url: jdbc:mysql://localhost/demo?autoReconnect=true&useSSL=false
    username: root
    password: 
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
  flyway:
    baseline-on-migrate: true
